# Portfólio - Entrega II

Projeto pronto para submissão: sistema de design, grid 12 colunas, responsividade, menu com dropdown e hambúrguer, componentes e validação de formulário.

Abra `index.html` no navegador ou use Live Server no VS Code.

Instruções rápidas para GitHub:
```
git init
git add .
git commit -m "Entrega II - portfolio completo"
git branch -M main
git remote add origin <URL-DO-REPO>
git push -u origin main
```
